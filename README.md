# AITrailRunningAssistant
An AI-powered Trail Running Assistant built in n8n that reads trail data, checks live weather, selects the best route, and generates a summary using GPT. It automates scheduling via Google Calendar and sends a full email breakdown, showcasing API integration and workflow automation.
